<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muslim_adel.sehaty">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".modules.base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modules.doctors.doctorsList.DoctorsListActivity"></activity>
        <activity android:name=".modules.doctors.reagons.ReagonsActivity" />
        <activity android:name=".modules.doctors.search.SearchBySpecialityActivity" />
        <activity android:name=".modules.doctors.specialties.SpecialtiesListActivity" />
        <activity android:name=".modules.doctors.search.SearchByNameActivity" />
        <activity
            android:name=".modules.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".modules.register.RegisterationActivity" />
        <activity android:name=".modules.register.LoginActivity" />
        <activity android:name=".modules.introSlider.IntroWizardActivity" />
        <activity android:name=".modules.home.MainActivity" />
        <activity android:name=".modules.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>